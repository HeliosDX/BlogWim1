
<form action="{{ path('heliosblog_modifier', {'id': article.id, 'blog': app.request.attributes.get('blog')}) }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}

    <input type="submit" class="btn btn-primary" />
</form>

  <p>
    Vous éditez un article déjà existant,
    ne le changez pas trop pour éviter
    aux membres de ne pas comprendre
    ce qu'il se passe.
  </p>

  <p>
    <a href="{{ path('heliosblog_voir', {'slug': article.slug, 'blog': app.request.attributes.get('blog')}) }}" class="btn">
      <i class="icon-chevron-left"></i>
      Retour à l'article
    </a>
  </p>