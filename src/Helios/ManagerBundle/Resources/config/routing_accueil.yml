# J'ai sorti cette route afin de ne pas avoir le préfixe /blog lors de l'import
heliosmanager_accueil:
    pattern:   /
    defaults:  { _controller: HeliosManagerBundle:Default:index }
    requirements:
        page:  \d+